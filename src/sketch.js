let Y, currentWidth, currentHeight;
function setup() {
    createCanvas(windowWidth, windowHeight);
    background(0);
    Y = windowHeight * 5 / 6;
}

function draw() {
    currentHeight = height;
    currentWidth = width;
}

